management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    metrics:
      tags:
        application: RegLogin
spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:9296